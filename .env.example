# Copie para .env e ajuste (arquivo .env nao e versionado):
#   cp .env.example .env

# Perfil AWS CLI (ex.: nova-conta)
AWS_PROFILE=nova-conta
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# Opcional: caminho do CSV de reviews
# DATA_CSV=data/Womens Clothing E-Commerce Reviews.csv
